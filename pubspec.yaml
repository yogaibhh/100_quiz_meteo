name: my_meteorology_quiz_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^0.13.5
  shared_preferences: ^2.1.0
  cupertino_icons: ^1.0.8
  intl: ^0.17.0 # Untuk format tanggal

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/questions.json
    - assets/images/skyhome.png
